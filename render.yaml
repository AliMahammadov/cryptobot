services:
  - type: web
    name: cryptosense-ai
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: AppConfig__MasterPin
        value: "2026"
